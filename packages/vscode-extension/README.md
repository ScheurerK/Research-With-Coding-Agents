# VS Code And Antigravity Extension

The Markplane interface extension is packaged as a VSIX during release. Install and update it only through IDE CLI commands using `--install-extension <vsix> --force`.
