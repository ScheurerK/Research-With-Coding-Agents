# VS Code And Antigravity Extension

The Markplane interface extension source lives in `source/`. Release builds package it as a VSIX and install or update it only through IDE CLI commands using `--install-extension <vsix> --force`.