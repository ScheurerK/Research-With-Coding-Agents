# Agent Adapters

Adapters connect the installed core to Codex, Claude Code, and Gemini/Antigravity.

- `hooks/` contains managed hook entrypoints and shared hook logic.
- `templates/` contains MCP configuration snippets installed by the agent-focused package.

Managed blocks must identify the bundled Research With Coding Agents Superpowers copy as authoritative and preserve unrelated configuration.