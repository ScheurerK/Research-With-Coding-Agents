---
description: Build all Markplane crates (cargo build --workspace)
---

Run `cargo build --workspace` from the repo root. Report any compile errors
with file:line references; do not attempt fixes unless asked.
