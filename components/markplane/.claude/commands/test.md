---
description: Run the full Markplane test suite (cargo test --workspace)
---

Run `cargo test --workspace` from the repo root. Summarize pass/fail counts
and list any failing test names with their file paths; do not attempt fixes
unless asked.
